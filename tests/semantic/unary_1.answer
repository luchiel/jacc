test_unary is function returning nothing defined as {
   (list)
   [
      i is variable of type <int> = (
         (0)
      )
      f is variable of type <float> = (
         (0.000000)
      )
   ]
    +-(@++) -> <int>
       |
       +-(var i) -> <int>

    +-(@--) -> <int>
       |
       +-(var i) -> <int>

    +-(@++) -> <float>
       |
       +-(var f) -> <float>

    +-(@--) -> <float>
       |
       +-(var f) -> <float>

    +-(++@) -> <int>
       |
       +-(var i) -> <int>

    +-(--@) -> <int>
       |
       +-(var i) -> <int>

    +-(++@) -> <float>
       |
       +-(var f) -> <float>

    +-(--@) -> <float>
       |
       +-(var f) -> <float>
}