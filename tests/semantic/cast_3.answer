char_test is function returning nothing defined as {
   (list)
   [
      c is variable of type <char> = (
         (cast to <char>)
          |
          +-(0)
      )
   ]
    +-(+) -> <char>
       |
       +-(var c) -> <char>
       |
       +-(cast to <char>)
          |
          +-(1)

    +-(+) -> <char>
       |
       +-(cast to <char>)
       |  |
       |  +-(1)
       |
       +-(var c) -> <char>

    +-(+) -> <int>
       |
       +-(cast to <int>)
       |  |
       |  +-(var c) -> <char>
       |
       +-(1)

    +-(+) -> <int>
       |
       +-(1)
       |
       +-(cast to <int>)
          |
          +-(var c) -> <char>

    +-(+) -> <float>
       |
       +-(cast to <float>)
       |  |
       |  +-(var c) -> <char>
       |
       +-(1.000000)

    +-(+) -> <float>
       |
       +-(1.000000)
       |
       +-(cast to <float>)
          |
          +-(var c) -> <char>
}